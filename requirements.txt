# QuantLib Pro - Streamlit Cloud Requirements
# Core dependencies for Streamlit Community Cloud deployment

# ── Data & Numerics ──────────────────────────────────────────
numpy>=1.26.4
pandas>=2.2.3
scipy>=1.12.0

# ── Market Data ──────────────────────────────────────────────
yfinance>=0.2.48
requests>=2.31.0

# ── Machine Learning ─────────────────────────────────────────
scikit-learn>=1.4.0
hmmlearn>=0.3.2

# ── Visualisation ────────────────────────────────────────────
matplotlib>=3.9.0
plotly>=5.20.0
seaborn>=0.13.0

# ── Computer Vision ──────────────────────────────────────────
# NOTE: Optional dependencies for advanced video features
# Comment out if deployment fails due to system dependencies
opencv-python-headless>=4.13.0
# mediapipe>=0.10.32  # Large dependency - uncomment if needed
# av>=16.1.0          # Video processing - uncomment if needed

# ── UI ───────────────────────────────────────────────────────
streamlit>=1.32.0
streamlit-autorefresh>=1.0.1
# streamlit-webrtc>=0.64.5  # WebRTC - uncomment if video streaming needed

# ── Graph / Network ──────────────────────────────────────────
networkx>=3.2.0

# ── Utilities ────────────────────────────────────────────────
python-dotenv>=1.0.0
pydantic>=2.6.0
# ── Backend Dependencies (Optional for Streamlit Cloud) ──────
# These are only needed if running the full API backend
# Comment out for lighter cloud deployments
# sqlalchemy>=2.0.0
# redis>=5.0.0
# psycopg2-binary>=2.9.0
# fastapi>=0.110.0
# uvicorn>=0.27.0
# ── Optional for cloud deployment ────────────────────────────
# Note: Redis and PostgreSQL will be optional in cloud deployment
# The app will gracefully degrade without database connections